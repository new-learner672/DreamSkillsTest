# DESIGN.md — 项目设计规范（模板）

> 使用方法：复制本文件到项目根目录，改名为 `DESIGN.md`，并按品牌/产品实际填写。
> 设计理念：结构化规范 > 自然语言。agent 无法执行"好看一点"，但能执行下列 tokens 与规则。
> Dreamskills 的 `dream-design-system` 与 `dream-ui-polish` 依赖本文件作为设计契约。

## 0. 设计关键词

- （示例：专业 · 克制 · 温暖）用 2–4 个形容词定义产品气质，作为所有视觉决策的"宪法"。

## 1. 色彩 Tokens

| Token | 值（示例） | 用途 |
|---|---|---|
| primary | #2563EB | 主操作、链接、激活态 |
| primary-hover | #1D4ED8 | 主操作悬停 |
| primary-fg | #FFFFFF | 主色上的文字 |
| surface | #FFFFFF | 卡片/面板背景 |
| surface-alt | #F8FAFC | 次级背景、斑马纹 |
| text | #0F172A | 正文 |
| text-secondary | #475569 | 次级文字 |
| border | #E2E8F0 | 分隔线、描边 |
| success / warning / danger | #16A34A / #F59E0B / #DC2626 | 语义反馈 |

**规则**：主色 ≤ 2 个（一个品牌主色 + 一个强调色）；中性色（surface/text/border）构成骨架；文字对比度 ≥ WCAG AA。

## 2. 字体与排版

- 字体族：（示例：正文 Inter / system-ui；标题可用同族加粗，避免超过 2 个字体族）
- 字号阶梯（px）：12 / 14 / 16 / 18 / 20 / 24 / 32 / 48
- 行高：正文 1.6；标题 1.2–1.3
- 字重：400（正文）/ 500（强调）/ 600（小标题）/ 700（标题）
- 规则：正文 ≥ 16px；每行 60–75 字符；段落间距 = 1em

## 3. 间距（8pt 网格）

- 刻度：4 / 8 / 16 / 24 / 32 / 48 / 64
- 规则：所有 padding/margin/gap 从刻度取值；卡片内边距 24；区块间距 32–48

## 4. 圆角与阴影

- 圆角：small 6 / medium 10 / large 14（按组件层级，勿混用多种圆角）
- 阴影：sm `0 1px 2px rgba(0,0,0,.05)`；md `0 4px 12px rgba(0,0,0,.08)`；lg `0 12px 32px rgba(0,0,0,.12)`

## 5. 动效

- 时长：150–300ms；缓动：ease-out（进入）/ ease-in-out（过渡）
- 规则：动效服务于反馈与引导，不做纯装饰动画；尊重 prefers-reduced-motion

## 6. 组件清单与用法

| 组件 | 来源 | 说明 |
|---|---|---|
| Button | registry（如 shadcn/ui）或自建 | 三级：primary / secondary / ghost |
| Input / Select | 同上 | 统一高度 40px、focus 态用 primary 描边 |
| Card | 同上 | 圆角 medium、阴影 sm |
| Modal / Dialog | 同上 | 阴影 lg、遮罩 rgba(15,23,42,.4) |

**规则**：优先复用 registry/现成组件（组件即资产、代码归你所有），确无合适者才自建，自建必须从本文件 tokens 派生。

## 7. 反 AI-slop 红线（dream-ui-polish 自检依据）

- ❌ 通篇灰底白卡、无视觉层次；❌ 五颜六色按钮；❌ 每块内容都塞进卡片；❌ 间距混乱无网格感；❌ 图标风格混搭；❌ 过度动效
- ✅ 有明确视觉重心；✅ 色彩克制且有品牌记忆点；✅ 留白呼吸感；✅ 排版层级清晰（标题/正文/辅助三档即可）
